-- Treesitter is now built into Neovim 0.10+
-- No plugin needed - just ensure parsers are installed
return {
  {
    "neovim/nvim-lspconfig",
    opts = {
      -- LSP config if needed
    },
  },
}
